'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _push = require('./push');

var _push2 = _interopRequireDefault(_push);

var _bump = require('../scripts/bump');

var _bump2 = _interopRequireDefault(_bump);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const name = 'bump';
const signature = `${name} <version>`;
const description = 'bump your project to a new <version>';

const register = program => program.command(signature, { noHelp: true }).usage('<version> [options]').description(description).withPushOptions().action(action);

async function action(version, options) {
  await (0, _bump2.default)({ version });
  await _push2.default.tryAction(options);
}

exports.default = { name, signature, description, register, action };