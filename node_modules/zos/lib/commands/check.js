'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _Truffle = require('../models/truffle/Truffle');

var _Truffle2 = _interopRequireDefault(_Truffle);

var _check = require('../scripts/check');

var _check2 = _interopRequireDefault(_check);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const name = 'check';
const signature = `${name} [contract]`;
const description = 'checks your contracts for potential issues';

const register = program => program.command(signature, { noHelp: true }).usage('[contract] [options]').description(description).option('--skip-compile', 'skips contract compilation').action(action);

async function action(contractAlias, options) {
  if (!options.skipCompile) await _Truffle2.default.compile();
  (0, _check2.default)({ contractAlias });
}

exports.default = { name, signature, description, register, action };