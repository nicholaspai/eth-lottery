'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _push = require('./push');

var _push2 = _interopRequireDefault(_push);

var _link = require('../scripts/link');

var _link2 = _interopRequireDefault(_link);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const name = 'link';
const signature = `${name} [dependencies...]`;
const description = 'links project with a list of dependencies each located in its npm package';

const register = program => program.command(signature, { noHelp: true }).usage('[dependencyName1 ... dependencyNameN] [options]').description(description).option('--no-install', 'skip installing packages dependencies locally').withPushOptions().action(action);

async function action(libs, options) {
  const installLibs = options.install;
  await (0, _link2.default)({ libs, installLibs });
  await _push2.default.tryAction(options);
}

exports.default = { name, signature, description, register, action };