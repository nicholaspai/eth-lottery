'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _remove = require('../scripts/remove');

var _remove2 = _interopRequireDefault(_remove);

var _push = require('./push');

var _push2 = _interopRequireDefault(_push);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const name = 'remove';
const signature = `${name} [contracts...]`;
const description = 'removes one or more contracts from your project. Provide a list of whitespace-separated contract names.';

const register = program => program.command(signature, { noHelp: true }).alias('rm').usage('[contract1 ... contractN] [options]').description(description).withPushOptions().action(action);

async function action(contracts, options) {
  (0, _remove2.default)({ contracts });
  await _push2.default.tryAction(options);
}

exports.default = { name, signature, description, register, action };