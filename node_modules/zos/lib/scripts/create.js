'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _stdout = require('../utils/stdout');

var _stdout2 = _interopRequireDefault(_stdout);

var _ControllerFor = require('../models/network/ControllerFor');

var _ControllerFor2 = _interopRequireDefault(_ControllerFor);

var _ScriptError = require('../models/errors/ScriptError');

var _ScriptError2 = _interopRequireDefault(_ScriptError);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = async function createProxy({ packageName, contractAlias, initMethod, initArgs, network, txParams = {}, force = false, networkFile = undefined }) {
  if (!contractAlias) throw Error('A contract alias must be provided to create a new proxy.');

  const controller = (0, _ControllerFor2.default)(network, txParams, networkFile);
  if (controller.isLib) throw Error('Cannot create a proxy for a package project');

  try {
    await controller.checkContractDeployed(packageName, contractAlias, !force);
    const proxy = await controller.createProxy(packageName, contractAlias, initMethod, initArgs);
    (0, _stdout2.default)(proxy.address);
    controller.writeNetworkPackageIfNeeded();

    return proxy;
  } catch (error) {
    const cb = () => controller.writeNetworkPackageIfNeeded();
    throw new _ScriptError2.default(error, cb);
  }
};